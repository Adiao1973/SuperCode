[package]
name = "supercode-cli"
description = "SuperCode CLI：Phase 0 原型宿主，驱动本机 agent CLI（当前仅 opencode）"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "supercode"
path = "src/main.rs"

[dependencies]
supercode-core.workspace = true
